<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      background: linear-gradient(to right, #9333ea, #3b82f6);
      color: white;
      padding: 30px;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }
    .content {
      background: #f9fafb;
      padding: 30px;
      border-radius: 0 0 8px 8px;
    }
    .credentials-list {
      background: white;
      border-radius: 8px;
      padding: 20px;
      margin: 20px 0;
    }
    .credential-item {
      padding: 10px 0;
      border-bottom: 1px solid #e5e7eb;
    }
    .credential-item:last-child {
      border-bottom: none;
    }
    .message-box {
      background: #eff6ff;
      border-left: 4px solid #3b82f6;
      padding: 15px;
      margin: 20px 0;
      border-radius: 4px;
    }
    .footer {
      text-align: center;
      padding: 20px;
      color: #6b7280;
      font-size: 12px;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background: linear-gradient(to right, #9333ea, #3b82f6);
      color: white;
      text-decoration: none;
      border-radius: 6px;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1 style="margin: 0;">Medical Credentials Shared</h1>
      <p style="margin: 10px 0 0 0;">MediVault</p>
    </div>

    <div class="content">
      <p>Hello,</p>

      <p><strong><%= @sender.full_name %></strong> (<%= @sender.email %>) has shared <%= @credentials.count %> medical credential(s) with you.</p>

      <% if @message.present? %>
        <div class="message-box">
          <strong>Message from <%= @sender.full_name %>:</strong>
          <p style="margin: 10px 0 0 0;"><%= simple_format(@message) %></p>
        </div>
      <% end %>

      <div class="credentials-list">
        <h3 style="margin-top: 0;">Included Credentials:</h3>
        <% @credentials.each do |credential| %>
          <div class="credential-item">
            <strong><%= credential.title %></strong><br>
            <small style="color: #6b7280;">
              Status: <%= credential.status.titleize %> |
              Expiry: <%= credential.end_date&.strftime('%B %d, %Y') || 'N/A' %>
            </small>
          </div>
        <% end %>
      </div>

      <p>All credentials have been compiled into a single PDF document attached to this email for your convenience.</p>

      <p style="color: #6b7280; font-size: 14px;">
        <strong>Please note:</strong> This bundle contains sensitive medical credential information.
        Please handle it securely and delete it after review if you're not the intended recipient.
      </p>
    </div>

    <div class="footer">
      <p>This email was sent via MediVault<br>
      © <%= Time.current.year %> MediVault. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
